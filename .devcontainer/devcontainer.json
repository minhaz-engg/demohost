{
  "name": "MarketIntel AI Host",
  "image": "mcr.microsoft.com/devcontainers/python:1-3.11-bookworm",
  "customizations": {
    "codespaces": {
      "openFiles": [
        "app.py"
      ]
    },
    "vscode": {
      "settings": {
        "python.defaultInterpreterPath": "/usr/local/bin/python"
      },
      "extensions": [
        "ms-python.python",
        "ms-python.vscode-pylance"
      ]
    }
  },
  "features": {
    "ghcr.io/devcontainers/features/node:1": {} 
  },
  "updateContentCommand": "sudo apt-get update && sudo apt-get install -y libasound2 libatk-bridge2.0-0 libgtk-3-0 libnss3 xdg-utils && pip3 install --user --upgrade pip && [ -f requirements.txt ] && pip3 install --user -r requirements.txt && crawl4ai-setup && echo '✅ Environment Ready - By the Will of Allah'",
  "postAttachCommand": {
    "server": "streamlit run app.py --server.enableCORS false --server.enableXsrfProtection false"
  },
  "portsAttributes": {
    "8501": {
      "label": "MarketIntel App",
      "onAutoForward": "openPreview"
    }
  },
  "forwardPorts": [
    8501
  ]
}